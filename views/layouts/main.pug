doctype html
html(lang='en')
    include head
    div(class="hero-body")
        div(class="container")
            h1(class="title")="Homepage"
            h2(class="subtitle")="It works!"

    body
        section(class="section")
            div(class="container")
                block content

    include footer

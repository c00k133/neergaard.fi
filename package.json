{
  "name": "theaa.team",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "scss": "node-sass assets/scss -o build/assets/css",
    "scss-watch": "node-sass --watch assets/scss -o build/assets/css",
    "pug": "pug templates --out build",
    "pug-watch": "pug --watch templates --out build",
    "build": "npm run scss && npm run pug"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "node-sass": "^4.13.0",
    "pug-cli": "^1.0.0-alpha6"
  }
}
